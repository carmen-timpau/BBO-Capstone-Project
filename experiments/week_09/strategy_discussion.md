**Discussion of Week 8 Query Outcomes:**

For **Function 1**, the output of Week 8's predicted query is -1.915098098611905e-109, which is incredibly small in comparison to the current global maximum for this function (7.710875114502849e-16). This indicates that Week 8's BBO pipeline still does not move closer towards global maximisation for this function specifically and the strategy still needs refinement. This could be because the Acquisition Function Ablation Study via Robust Multi-Step Sequential Offline Bayesian Optimization Rollout employed in Week 8's strategy exhausted the finite candidate pool almost every time (including function 1). This makes every acquisition function tested in the ablation study to be eventually forced to visit every remaining point within the dataset, collapsing for most functions (including Function 1) to an artificial 0.0 final simple regret, _making the primary ranking metric uninformative for the acquisition ablation_ and defaulting to use the secondary Area Under the Regret Curve (AURC) metric (implemented as a tiebreaker for sparse cases not the intended default metric), that captures convergence speed, which is not the optimal metric when the overarching goal of the algorithm is global function maximisation (because AURC may prefer a route which may never converge). This candidate pool exhaustion problem will be immediately dealt with in Week 9, by implementing a **Holdout Fraction (30%) Cap on Rollout Iterations**, to prevent the rollout from exhausting the finite candidate pool and _ensure that this leads to truly meaningful rankings based on real primary performance metric results_.

For **Function 2**, the output of Week 8's predicted query is -0.012640477089899252, which is not exceeding the existing maximum for this function  (0.7560218355559285) and actually sits fairly far away from this current maximum output value. This could be because of the issue of exhausting the finite candidate pool mentioned above which will be fixed this week (Week 9), but does not necessarily mean that the current pipeline is heavily underperforming, as the global maximum may have potentially already been identified for this function. Therefore, this resulted output in isolation cannot clearly inform whether the pipeline is moving towards a favourable direction or not. Regardless, the pipeline will be improved in Week 9 to ensure that the predicted queries' quality keeps improving as well, from week to week (see Week 9 Strategy Summary in `week_09/strategy_summary.md`).

For **Function 3**, the output of Week 8's predicted query is -0.04901329786602244, which is not exceeding the existing maximum for this function  (-0.034835313350078584), albeit it is fairly close to it. This does not necessarily mean that the pipeline is underperforming, as the global maximum may have potentially already been identified. Therefore, this resulted output in isolation cannot clearly inform whether the pipeline is moving towards a favourable direction or not. Regardless, the pipeline will be improved in Week 9 to ensure that the predicted queries' quality keeps improving as well, from week to week.

For **Function 4**, the output of Week 8's predicted query is -12.332893692566234, which is not exceeding the existing maximum for this function  (-0.5823883600251594) and actually sits very far away from this current maximum output value. This could be due to exhausting the finite candidate pool almost every time during acquisition ablation (including function 4), issue detailed above within the output discussion for Function 1. This problem will be dealt with in Week 9, by implementing a **Holdout Fraction (30%) Cap on Rollout Iterations**, to prevent the rollout from exhausting the finite candidate pool and lead to truly meaningful rankings based on real performance.

For **Function 5**, the output of Week 8's predicted query is 2333.006822317816, which is exactly the same output obtained in the previous 2 weeks (Weeks 6 and 7), because the same exact input was predicted as the next query by the ML pipeline despite all changes made and therefore submitted to query the function in all 3 consecutive weeks (Weeks 6-8). This may potentially be happening because the algorithm has truly identified the global maximum and remained stuck there. Regardless, submitting exact past queries wastes expensive resources and therefore this specific issue will be dealt with in this week (Week 9), so it stops happening and allows for new data to be acquired for this function, even if the outputs obtained in the future will be lower than this potential global maximum value obtained repeatedly so far.

For **Function 6**, the output of Week 8's predicted query is -0.7783429769692322, which is not exceeding the existing maximum for this function (-0.2598794432558996). This does not necessarily mean that the pipeline is underperforming, as the global maximum may have potentially already been identified. Therefore, this resulted output in isolation cannot clearly inform whether the pipeline is moving towards a favourable direction or not. Regardless, the pipeline will be improved in Week 9 to ensure that the predicted queries' quality keeps improving as well, from week to week.

For **Function 7**, the output of Week 8's predicted query is 0.09278551505595779, which is not exceeding the existing maximum for this function (1.53655724155759). This does not necessarily mean that the pipeline is underperforming, as the global maximum may have potentially already been identified. Therefore, this resulted output in isolation cannot clearly inform whether the pipeline is moving towards a favourable direction or not. Regardless, the pipeline will be improved in Week 9 to ensure that the predicted queries' quality keeps improving as well, from week to week.

For **Function 8**, the output of Week 8's predicted query is 9.5154049468915, which is not exceeding the existing maximum for this function (9.9561276549796). This does not necessarily mean that the pipeline is underperforming, as the global maximum may have potentially already been identified. Therefore, this resulted output in isolation cannot clearly inform whether the pipeline is moving towards a favourable direction or not. Regardless, the pipeline will be improved in Week 9 to ensure that the predicted queries' quality keeps improving as well, from week to week.
